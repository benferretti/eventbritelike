<br><br><br>
<h1 class="h3 mb-3 font-weight-normal text-center">Liste des utilisateurs du site:</h1>
<br>

<div class="container">

      <div class="card-body text-center">
            <div class="list-group" id="list-tab" role="tablist">
                <% @users.each do |user| %>
                    
                        <%= link_to user.email, admin_user_path(:id => user.id), class: "list-group-item list-group-item-action" %>
                 
                <% end %>
            </div>
    